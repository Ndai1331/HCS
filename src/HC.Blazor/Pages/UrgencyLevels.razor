@page "/urgency-levels"
@attribute [Authorize(HCPermissions.MasterDatas.UrgencyLevelDefault)]
@inherits MasterDataPage

@* Render the inherited base component markup; without this, Razor emits an empty BuildRenderTree override and the page shows nothing. *@
@{
    base.BuildRenderTree(__builder);
}

@code {
    protected override string DefaultType => "MUC_DO_KHAN";
    protected override string PageTitle => L["UrgencyLevel"];
    protected override string PageRoute => "/urgency-levels";
}
