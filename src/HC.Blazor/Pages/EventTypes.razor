@page "/even-types"
@attribute [Authorize(HCPermissions.MasterDatas.EventTypeDefault)]
@inherits MasterDataPage

@* Render the inherited base component markup; without this, Razor emits an empty BuildRenderTree override and the page shows nothing. *@
@{
    base.BuildRenderTree(__builder);
}

@code {
    protected override string DefaultType => "LOAI_SU_KIEN";
    protected override string PageTitle => L["EventType"];
    protected override string PageRoute => "/even-types";
}
