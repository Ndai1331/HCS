@page "/confidentiality-levels"
@attribute [Authorize(HCPermissions.MasterDatas.ConfidentialityLevelDefault)]
@inherits MasterDataPage

@* Render the inherited base component markup; without this, Razor emits an empty BuildRenderTree override and the page shows nothing. *@
@{
    base.BuildRenderTree(__builder);
}

@code {
    protected override string DefaultType => "MUC_DO_MAT";
    protected override string PageTitle => L["ConfidentialityLevel"];
    protected override string PageRoute => "/confidentiality-levels";
}
