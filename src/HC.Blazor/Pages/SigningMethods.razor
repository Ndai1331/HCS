@page "/signing-methods"
@attribute [Authorize(HCPermissions.MasterDatas.SigningMethodDefault)]
@inherits MasterDataPage

@* Render the inherited base component markup; without this, Razor emits an empty BuildRenderTree override and the page shows nothing. *@
@{
    base.BuildRenderTree(__builder);
}

@code {
    protected override string DefaultType => "LOAI_KY";
    protected override string PageTitle => L["SigningMethod"];
    protected override string PageRoute => "/signing-methods";
}
