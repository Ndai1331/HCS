@page "/processing-methods"
@attribute [Authorize(HCPermissions.MasterDatas.ProcessingMethodDefault)]
@inherits MasterDataPage

@* Render the inherited base component markup; without this, Razor emits an empty BuildRenderTree override and the page shows nothing. *@
@{
    base.BuildRenderTree(__builder);
}

@code {
    protected override string DefaultType => "HINH_THUC_XL";
    protected override string PageTitle => L["ProcessingMethod"];
    protected override string PageRoute => "/processing-methods";
}
