FROM mcr.microsoft.com/dotnet/sdk:10.0

# Keep heavy OS dependencies in a reusable base layer.
# DejaVu / Noto: Vietnamese glyphs for PDFsharp signing (see PdfFontEnvironment.DefaultPdfFontFamily).
RUN apt-get update && apt-get install -y \
    libreoffice \
    libreoffice-writer \
    libreoffice-core \
    python3 \
    python3-uno \
    unoconv \
    fontconfig \
    fonts-dejavu \
    fonts-dejavu-core \
    fonts-dejavu-extra \
    fonts-noto-core \
    fonts-noto \
    libfreetype6 \
    libfontconfig1 \
 && rm -rf /var/lib/apt/lists/* \
 && fc-cache -fv
